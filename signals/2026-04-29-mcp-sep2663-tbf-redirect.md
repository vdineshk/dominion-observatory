# MCP SEP-2663 TBF Redirect Signal — 2026-04-29

## Source
modelcontextprotocol/modelcontextprotocol#2663 (Tasks Extension)
Reply from @LucaButBoring to @vdineshk

## Raw Signal
> "@vdineshk Most of the same feedback applies to your suggestion as well, although in the case
> of TBF that feels more like something that the registry API or potentially server cards should
> be exposing, rather than something that could be directly attached to this SEP. In any case,
> much like tasks/steer, TBF would be an expansion of scope that would be largely orthogonal to
> the core goals of tasks right now (that is, durable request execution and snapping onto
> workflow APIs)."
>
> "I would welcome a formal SEP for that, regardless of whether it builds on top of this one
> or if it runs separately."

## Strategic Read
POSITIVE REDIRECT. Not rejection — venue correction by core MCP spec maintainer.

Key signals:
1. "registry API or server cards" = Observatory's exact surface named by maintainer
2. "I would welcome a formal SEP for that" = explicit invitation
3. Tuesday Agents WG meeting flagged — adjacent agent modeling discussion in motion

## Pattern-025 Instance #2
AGT: "community-standard trust protocol" → MCP: "registry API/server cards + formal SEP"
Both point to same answer: write the spec, be the reference implementation.

## Next Action (highest leverage)
Draft formal SEP for behavioral trust framework as MCP registry API extension.
File as new SEP in modelcontextprotocol/modelcontextprotocol.
Observatory /v1/behavioral-evidence + /.well-known/mcp-observatory = live reference implementation.

## DAEE-Intelligence
Entry: MCP-SEP-2663-TBF-REDIRECT-2026-04-29
